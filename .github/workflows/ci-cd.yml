name: CI/CD Pipeline

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

env:
  NODE_VERSION: '20'
  PNPM_VERSION: '9'

jobs:
  # 代码质量检查
  lint:
    name: Lint Code
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v2
        with:
          version: ${{ env.PNPM_VERSION }}

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Run ESLint
        run: pnpm lint

  # 自动化测试
  test:
    name: Run Tests
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v2
        with:
          version: ${{ env.PNPM_VERSION }}

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Run tests
        run: pnpm test:run

      - name: Generate coverage report
        run: pnpm test:coverage
        continue-on-error: true

      - name: Upload coverage reports
        uses: codecov/codecov-action@v4
        if: always()
        with:
          files: ./coverage/coverage-final.json
          flags: unittests
          name: codecov-umbrella
          fail_ci_if_error: false

  # 构建项目
  build:
    name: Build Project
    runs-on: ubuntu-latest
    needs: [lint, test]
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v2
        with:
          version: ${{ env.PNPM_VERSION }}

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Verify environment and paths
        run: |
          echo "🔍 Environment check:"
          echo "NODE_ENV: $NODE_ENV"
          echo "PWD: $(pwd)"
          echo "📁 Project structure:"
          ls -la | head -20
          echo "📁 Public directory:"
          [ -d "public" ] && ls -la public/ | head -10 || echo "⚠️  public directory not found"
          echo "📄 posts-list.json:"
          [ -f "public/posts-list.json" ] && echo "✅ Found" || echo "⚠️  Not found"
          echo "📁 docs directory:"
          [ -d "public/docs" ] && echo "✅ Found ($(find public/docs -name '*.md' | wc -l) markdown files)" || echo "⚠️  Not found"

      - name: Build project
        run: |
          export NODE_ENV=production
          echo "🔨 Building with NODE_ENV=$NODE_ENV"
          echo "📦 Running: pnpm build"
          pnpm build || {
            echo "❌ Build failed!"
            echo "📋 Checking partial build output..."
            if [ -d "out" ]; then
              echo "📁 Partial out directory contents:"
              find out -type f | head -20
              echo "📁 Directories in out:"
              ls -la out/ | head -20
            fi
            exit 1
          }
        env:
          NODE_ENV: production

      - name: Check build output
        run: |
          if [ ! -d "out" ]; then
            echo "❌ Build failed - out directory not found"
            exit 1
          fi
          echo "✅ Build successful - Static files generated in out/"
          echo "📊 Build statistics:"
          echo "  Total files: $(find out -type f | wc -l)"
          echo "  Total directories: $(find out -type d | wc -l)"
          echo "📁 Required directories check:"
          REQUIRED_DIRS=("about" "ai" "auth" "blog" "docs" "images" "login" "post" "practice" "404" "_next")
          MISSING_DIRS=()
          for dir in "${REQUIRED_DIRS[@]}"; do
            if [ -d "out/$dir" ]; then
              echo "  ✅ out/$dir"
            else
              echo "  ❌ out/$dir (missing)"
              MISSING_DIRS+=("$dir")
            fi
          done
          if [ ${#MISSING_DIRS[@]} -gt 0 ]; then
            echo "❌ Missing directories: ${MISSING_DIRS[*]}"
            echo "📋 All directories in out/:"
            ls -la out/
            exit 1
          fi
          if [ ! -f "out/index.html" ]; then
            echo "❌ Missing out/index.html"
            exit 1
          fi
          echo "✅ All required directories and files present"

      - name: Upload build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: static-files
          path: out/
          retention-days: 7
          if-no-files-found: error

  # 部署到服务器
  deploy:
    name: Deploy to Server
    runs-on: ubuntu-latest
    needs: [build]
    if: github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master'
    environment:
      name: production
    steps:
      - name: Download build artifacts
        uses: actions/download-artifact@v4
        with:
          name: static-files
          path: .
          merge-multiple: false

      - name: Verify build artifacts
        run: |
          # 检查下载后的文件结构
          echo "📁 Current directory contents:"
          ls -la
          
          # 检查 out 目录是否存在
          if [ -d "out" ]; then
            echo "✅ Found out directory"
            echo "📁 Contents of out/:"
            ls -la out/ | head -20
          elif [ -f "index.html" ]; then
            echo "⚠️  Artifact files are in root directory, creating out/ structure..."
            mkdir -p out
            # 移动所有静态文件到 out 目录
            find . -maxdepth 1 -type f \( -name "*.html" -o -name "*.txt" -o -name "*.json" -o -name "*.xml" -o -name "*.pdf" \) ! -name ".gitignore" -exec mv {} out/ \;
            for dir in _next docs post about ai auth blog images login practice 404; do
              [ -d "$dir" ] && mv "$dir" out/ || true
            done
            echo "✅ Reorganized files into out/ directory"
          else
            echo "❌ Build artifacts not found"
            echo "📋 Listing all files:"
            find . -type f | head -20
            exit 1
          fi
          
          # 最终验证
          if [ ! -d "out" ]; then
            echo "❌ Build artifacts not found - out directory missing"
            exit 1
          fi
          if [ ! -f "out/index.html" ]; then
            echo "❌ Build artifacts incomplete - index.html missing"
            echo "📋 Files in out/:"
            ls -la out/ | head -20
            exit 1
          fi
          
          # 检查所有必需的目录
          echo "🔍 Verifying required directories in artifacts..."
          REQUIRED_DIRS=("about" "ai" "auth" "blog" "docs" "images" "login" "post" "practice" "404" "_next")
          MISSING_DIRS=()
          for dir in "${REQUIRED_DIRS[@]}"; do
            if [ -d "out/$dir" ]; then
              FILE_COUNT=$(find "out/$dir" -type f | wc -l)
              echo "  ✅ out/$dir ($FILE_COUNT files)"
            else
              echo "  ❌ out/$dir (missing)"
              MISSING_DIRS+=("$dir")
            fi
          done
          
          if [ ${#MISSING_DIRS[@]} -gt 0 ]; then
            echo "❌ Missing directories in artifacts: ${MISSING_DIRS[*]}"
            echo "📋 All directories in out/:"
            ls -la out/
            exit 1
          fi
          
          echo "✅ Build artifacts verified - Static files ready for deployment"
          echo "📊 Files count in out/: $(find out -type f | wc -l)"
          echo "📊 Directories count in out/: $(find out -type d | wc -l)"

      - name: Prepare deployment directory
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USERNAME }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: ${{ secrets.SSH_PORT || 22 }}
          script: |
            DEPLOY_PATH="${{ secrets.DEPLOY_PATH || '/www/wwwroot/lanlangmozhu.com' }}"
            # 确保部署目录存在
            mkdir -p $DEPLOY_PATH
            echo "✅ Deployment directory exists: $DEPLOY_PATH"
            # 清理旧文件（只删除静态文件，保留其他重要文件）
            if [ -d "$DEPLOY_PATH" ]; then
              echo "🧹 Cleaning old static files..."
              # 删除旧的 HTML、JS、CSS 等静态文件，但保留 .htaccess、.user.ini 等配置文件
              find $DEPLOY_PATH -maxdepth 1 -type f \( -name "*.html" -o -name "*.txt" -o -name "*.json" -o -name "*.xml" -o -name "*.pdf" \) ! -name ".htaccess" ! -name ".user.ini" -delete 2>/dev/null || true
              # 删除所有 Next.js 生成的目录
              echo "🗑️  Removing old Next.js directories..."
              for dir in about ai auth blog docs images login post practice 404 _next; do
                if [ -d "$DEPLOY_PATH/$dir" ]; then
                  rm -rf "$DEPLOY_PATH/$dir"
                  echo "  ✅ Removed: $dir"
                fi
              done
              echo "✅ Old files cleaned"
            fi

      - name: List files before deployment
        run: |
          echo "📦 Preparing deployment..."
          echo "📁 Directories to deploy:"
          ls -la out/ | head -20
          echo "📊 Total files: $(find out -type f | wc -l)"
          echo "📊 Total directories: $(find out -type d | wc -l)"
          echo "🔍 Final check - key directories and files:"
          for dir in blog ai practice about login auth 404; do
            if [ -d "out/$dir" ]; then
              FILE_COUNT=$(find "out/$dir" -type f | wc -l)
              echo "  ✅ out/$dir ($FILE_COUNT files)"
              if [ -f "out/$dir/index.html" ]; then
                echo "     ✅ index.html exists"
              else
                echo "     ❌ index.html missing"
              fi
            else
              echo "  ❌ out/$dir (missing) - DEPLOYMENT WILL FAIL"
            fi
          done
          echo "📋 Sample files in out/:"
          find out -type f | head -20

      - name: Create deployment archive
        run: |
          echo "📦 Creating tar archive from out/ directory..."
          cd out
          tar czf ../deploy.tar.gz .
          cd ..
          echo "✅ Archive created: $(ls -lh deploy.tar.gz | awk '{print $5}')"
          echo "📊 Archive contents preview:"
          tar tzf deploy.tar.gz | head -30
          
      - name: Transfer archive to server
        uses: appleboy/scp-action@master
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USERNAME }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: ${{ secrets.SSH_PORT || 22 }}
          source: "deploy.tar.gz"
          target: "/tmp/"
          rm: false
          overwrite: true
          
      - name: Extract and deploy files
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USERNAME }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: ${{ secrets.SSH_PORT || 22 }}
          script: |
            DEPLOY_PATH="${{ secrets.DEPLOY_PATH || '/www/wwwroot/lanlangmozhu.com' }}"
            echo "📦 Extracting files to $DEPLOY_PATH..."
            cd $DEPLOY_PATH
            tar xzf /tmp/deploy.tar.gz
            rm -f /tmp/deploy.tar.gz
            echo "✅ Files extracted successfully"
            echo "📁 Deployed directories:"
            ls -la | head -20
            echo "📊 File count: $(find . -type f | wc -l)"

      - name: Set correct permissions
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USERNAME }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: ${{ secrets.SSH_PORT || 22 }}
          script: |
            cd ${{ secrets.DEPLOY_PATH || '/www/wwwroot/lanlangmozhu.com' }}
            # 设置文件权限（宝塔面板通常使用 www 用户）
            chown -R www:www . || chown -R $USER:$USER .
            find . -type f -exec chmod 644 {} \;
            find . -type d -exec chmod 755 {} \;
            echo "✅ Permissions set successfully"

      - name: Verify deployment
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USERNAME }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: ${{ secrets.SSH_PORT || 22 }}
          script: |
            DEPLOY_PATH="${{ secrets.DEPLOY_PATH || '/www/wwwroot/lanlangmozhu.com' }}"
            cd $DEPLOY_PATH
            
            # 检查关键文件是否存在
            if [ ! -f "index.html" ]; then
              echo "❌ Deployment verification failed - index.html not found"
              exit 1
            fi
            
            # 检查必需的目录
            echo "📁 Checking required directories..."
            REQUIRED_DIRS=("about" "ai" "auth" "blog" "docs" "images" "login" "post" "practice" "404" "_next")
            MISSING_DIRS=()
            for dir in "${REQUIRED_DIRS[@]}"; do
              if [ -d "$dir" ]; then
                echo "  ✅ $dir/"
                # 检查目录中是否有文件
                FILE_COUNT=$(find "$dir" -type f | wc -l)
                echo "     Files: $FILE_COUNT"
              else
                echo "  ❌ $dir/ (missing)"
                MISSING_DIRS+=("$dir")
              fi
            done
            
            if [ ${#MISSING_DIRS[@]} -gt 0 ]; then
              echo "❌ Missing directories: ${MISSING_DIRS[*]}"
              echo "📋 Current directory structure:"
              ls -la
              exit 1
            fi
            
            # 检查关键页面文件
            echo "📄 Checking key page files..."
            KEY_PAGES=("blog/index.html" "ai/index.html" "practice/index.html" "about/index.html" "login/index.html")
            MISSING_PAGES=()
            for page in "${KEY_PAGES[@]}"; do
              if [ -f "$page" ]; then
                echo "  ✅ $page"
              else
                echo "  ❌ $page (missing)"
                MISSING_PAGES+=("$page")
              fi
            done
            
            if [ ${#MISSING_PAGES[@]} -gt 0 ]; then
              echo "❌ Missing page files: ${MISSING_PAGES[*]}"
              exit 1
            fi
            
            # 检查文件数量
            FILE_COUNT=$(find . -type f | wc -l)
            DIR_COUNT=$(find . -type d | wc -l)
            echo "📊 Deployment statistics:"
            echo "  Total files: $FILE_COUNT"
            echo "  Total directories: $DIR_COUNT"
            
            echo "✅ Deployment verification successful!"

      - name: Deployment notification
        if: success()
        run: |
          echo "✅ Static website deployed successfully to Baota Panel!"
          echo "📦 Deployment path: ${{ secrets.DEPLOY_PATH || '/www/wwwroot/lanlangmozhu.com' }}"

